# daily

# sudo chmod +x /usr/local/bin/burp                        
#!/bin/bash
#Make sure the path below points to where YOUR jar file actually is
java -jar /home/tehang07/path/to/your/burp-loader.jar "$@"
